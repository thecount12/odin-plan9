# 9front Odin stubs

Plan 9 **C runtime** lives in [`../plan9/`](../plan9/) (Phase 7).

This directory holds Odin-language bindings (`os_9front.odin`) for when the compiler targets 9front directly.

The old placeholder `mmap.c` / `mmap.h` here are superseded by the plan9 port — do not extend them.
